Inverse (problematic) prompts for the Obama / celebrity concept.

After training, `train_obama.py` writes:

- `Obama_eta_{η}_K_{length}.csv` here, and
- `/kaggle/working/Obama_invprompts.csv` when run on Kaggle.
