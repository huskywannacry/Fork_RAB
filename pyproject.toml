[project]
name = "ring-a-bell"
version = "0.1.0"
description = "Ring-A-Bell concept extraction and inverse-prompt search (Obama / celebrity)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "torch",
    "tqdm",
    "transformers",
]
